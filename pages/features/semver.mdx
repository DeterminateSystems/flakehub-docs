# Semantic versioning

FlakeHub provides semantic versioning (SemVer) for Nix flakes.

For more information on this feature, see our [concept doc][semver] on SemVer for Nix.

[semver]: ../concepts/semver
